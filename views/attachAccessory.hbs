<h1>Attach a new accessory</h1>
<div class="form">
    <h2>{{guitar.name}}</h2>
    <img class="cube" src="{{guitar.imageUrl}}">
    {{#if accessories}}
    <form action="/attach/{{guitar.id}}" method="POST">
        <label for="accessory">Accessories</label>
        <select id="accessory" name="accessory" />
        {{#each accessories}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
        </select>
        <input type="submit" value="Attach">
        <a href="/details/{{guitar.id}}" class="cancel">Cancel</a>
    </form>
    {{else}}
    <h3 class="italic">
        This guitar has all available accessories so far or there is no available at all...
        </h3>
    {{/if}}
</div>